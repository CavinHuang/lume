# Lume v0.1.6 托盘与窗口验证记录

> 状态：实现与自动化验收已完成。平台截图不作为本计划关闭条件，后续发布时可按需补充。

## 自动化证明

- `apps/desktop/scripts/tray-window-runtime.test.mjs`：覆盖并发 single-flight、generation 隔离、ready 前关闭无未处理 rejection、后续重建、last-intent-wins、revision 重置、托盘销毁失败、sender 校验、payload 边界和 10 秒拒绝日志限流。
- `apps/desktop/scripts/desktop-core.test.mjs`：覆盖 Windows/macOS 窗口策略、托盘不可用降级、菜单顺序、最近会话前缀和标题宽度。
- `apps/desktop/scripts/electron-security.test.mjs`：覆盖 renderer/preload allowlist 一致性与 IPC sender 安全边界。
- `apps/web/src/components/app-shell/LeftSidebar.test.ts`：覆盖本地陈旧/后端已删除、可见签名不变仍强制同步、成功确认、查询失败和超时 fail closed。

## Windows 实机

- 系统报告：Windows Product Name `Windows 10 Pro`，build `26200`，x64。
- 显示器：2560×1440；系统 Applied DPI `120`（125%）。
- 主题：应用与系统均为暗色。
- 开发构建：桌面 runtime 构建成功；新实例在 125% scale factor 下启动并完成 `tray ready`、sidecar ready 与 main window ready，日志中没有 `tray.create_failed` 或 `window.create_failed`。
- 主窗口：Computer Use 捕获确认 1440×920 窗口能够正常显示、聚焦并暴露窗口控制。
- 最小化行为：在 `showTray=true`、`minimizeToTray=true` 下点击最小化后，主窗口从可见窗口列表消失，Electron 主进程保持运行；重启开发实例后窗口再次正常创建。
- 发布时可选补充：托盘 Logo、最近 5 条菜单宽度、当前项 `✓` 前缀、显示/隐藏与窗口重建截图。

## macOS 实机

- 发布时可选补充：系统版本、Intel/Apple Silicon、Retina 缩放和明暗主题。
- 发布时可选补充：Template Image 在浅色/深色菜单栏中的可见性、菜单宽度、当前项 `✓` 前缀和关闭窗口后的 Dock/托盘重建。
- 安装流程：双击 DMG → 拖入 Applications → 双击应用；仅被 Gatekeeper 阻止时才 Control-click。

## 完成条件

- [x] 生命周期与 IPC 行为测试通过（desktop 84 项、web 10 项）。
- [x] Windows 系统、DPI 和主题信息已记录。
- [x] 计划关闭；Windows/macOS 截图转为发布时可选证据。
