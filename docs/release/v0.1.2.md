# Lume v0.1.2

> 修复 Windows 安装后 sidecar 因引用 CI 构建机路径而无法启动的问题。

## 🔥 Highlights

- 🐛 启动可靠性: jsdom 默认样式表现在直接嵌入 sidecar 产物，安装目录和构建环境不再影响启动。

## 🐛 Bug Fixes

- fix(sidecar): 移除 jsdom 对 CI 工作目录中 `default-stylesheet.css` 的运行时依赖。
- test(sidecar): 打包 smoke 检查会拒绝残留构建机样式表路径的产物。

---

## 📊 变更概览

- 1 个分类 · 1 个修复 commit
- 上一个版本: v0.1.1
