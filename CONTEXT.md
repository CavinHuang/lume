# Lume 对话输入域

本上下文定义 Lume 各输入入口在编辑、引用、提交和排队过程中的统一语言，避免把临时输入状态与已经进入会话生命周期的消息混为一谈。

## Language

**输入草稿（Composer Draft）**：
尚未成功提交的富文本内容，包括普通文本和能力引用；欢迎页草稿归属于所选工作区，会话页与 Quick Input 草稿归属于当前会话。附件、剪贴板图片、桌面上下文和已排队消息不属于输入草稿。
_Avoid_: 待发送消息、队列项、附件草稿

**待提交附件（Pending Attachment）**：
已经加入当前输入框、但尚未被一次提交正式接收的文件或图片。它是当前输入会话的临时状态，不属于输入草稿，也不是已发送消息附件。
_Avoid_: 附件草稿、已上传附件、消息附件

**可用附件（Consumable Attachment）**：
不仅已经显示在消息中，而且当前会话确实能够把内容交给模型或通过获准能力读取的附件。
_Avoid_: 仅展示成功、静默忽略的附件、不可消费附件

**逻辑提交（Logical Submission）**：
用户针对一份不可变输入载荷发起的一次提交意图；结果未知时的传输重试仍属于同一次逻辑提交，明确拒绝或用户修改内容后则是新的逻辑提交。
_Avoid_: 单次 RPC、消息、运行

**提交收据（Submission Receipt）**：
记录逻辑提交是否被系统接收及其后续状态的持久凭证。提交收据不是用户消息，也不表示 Agent 已经开始或完成运行。
_Avoid_: 消息回执、运行结果、发送请求

**新会话输入态（New-session Composer）**：
尚未创建会话、但允许用户编辑第一条输入的状态；欢迎页和 Quick Input 都可以处于该状态。只有第一条逻辑提交被接收后，它才归属于一个正式会话。
_Avoid_: 空会话、临时会话、预创建会话

**消息文件引用协议 V1（Message File Reference Protocol V1）**：
Agent 消息中表达项目或会话文件位置的首个稳定协议版本；历史消息按其生成时的协议版本解释，不由后续语法重新定义。
_Avoid_: 当前路径格式、无版本文件链接

**绑定保护（Binding Guard）**：
消息文件引用携带的绑定一致性约束，用于确认引用仍属于生成消息时的项目或会话上下文；它不是对受控 renderer 的不可伪造授权能力。
_Avoid_: 授权 capability、安全令牌、签名文件引用

**当前绑定导航（Current-Binding Navigation）**：
用户进入 Files 后按该工作区当前的项目或会话绑定浏览文件；消息的绑定保护只约束从消息发起的首次定位，不会永久附着到整棵文件树。
_Avoid_: 继承消息授权的文件树、永久 guarded 浏览

**主输入保留区（Composer Reserve）**：
辅助面板出现或调整时，仍为主对话与输入框保留的可用区域。临时的布局收缩不改变用户保存的导航偏好。
_Avoid_: 允许辅助面板遮挡输入框、把临时收缩保存为用户偏好

**阻塞队首（Blocked Queue Head）**：
重新校验失败后暂停队列推进的第一条排队消息。系统不会自动越过它；只有用户明确移除或重排后，后续消息才可以继续。
_Avoid_: 自动跳过、把重排当作删除
