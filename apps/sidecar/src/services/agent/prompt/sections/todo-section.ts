export function buildTodoSection(): string {
  return `## TodoWrite — session task list
Use for multi-step work with 3+ distinct steps or multiple user-provided tasks; skip for single trivial or purely conversational requests.
Keep exactly one task in_progress and mark tasks completed the moment they are done.`;
}
