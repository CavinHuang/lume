{
  "name": "@lume/desktop",
  "version": "0.0.5",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tauri dev --config src-tauri/tauri.dev.conf.json",
    "build": "bun ../../scripts/build-natives-binary.mjs && bun ../../scripts/build-sidecar-binary.mjs && bun ../../scripts/build-default-skills-archive.mjs && tauri build --bundles app",
    "package": "bun ../../scripts/build-natives-binary.mjs && bun ../../scripts/build-sidecar-binary.mjs && bun ../../scripts/build-default-skills-archive.mjs && tauri build --config src-tauri/tauri.release.conf.json",
    "tauri": "tauri",
    "typecheck": "echo \"desktop has no ts sources yet\""
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
