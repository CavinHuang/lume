# Design: Lume 前端完整架构

Generated by /office-hours on 2026-04-16
Branch: feat/open-agent-sdk
Repo: lume
Status: DRAFT
Mode: Builder

## Problem Statement

Lume 是一个开源桌面 Agent 项目，后端（sidecar）已完整实现：Agent runtime、多 provider（Anthropic/OpenAI/Google/国内厂商）、MCP、Skills、Plan 模式、子任务代理、文件系统、工作区管理。

前端 `apps/web` 目前**不存在，需要从零新建**。目标：做成 Claude Code 桌面版体验 — 流式工具执行可视化、权限控制、多标签页、文件浏览器、子任务面板。

## What Makes This Cool

- 把 Claude Code CLI 的体验搬到 GUI：工具执行实时可视化、diff 视图、bash 输出、权限确认
- 多标签页 Agent 会话，可以同时跑多个任务
- 子任务代理内联卡片 — 子任务进度直接在对话流中可见，不割裂上下文
- Plan 模式：规划 → 审核 → 执行，每步可见
- 工作区文件浏览器，Agent 改了什么文件一目了然

## Constraints

- Tauri 2.0（非 Electron），IPC 通过 `@tauri-apps/api/core` invoke + `@tauri-apps/api/event` listen
- 前端在 `apps/web`（**目前不存在，全新创建**），dev URL `127.0.0.1:3000`，打包输出 `apps/web/dist`
- 技术栈：React + TypeScript + Tailwind + Jotai + shadcn/ui
- `packages/shared` 已有完整类型定义和 IPC 通道常量（`AGENT_IPC_CHANNELS`）
- `packages/ui` 已有：`CodeBlock`（Shiki 高亮）、`MermaidBlock`、`useSmoothStream`（流式动画）
- shadcn/ui 基础组件在 `apps/web/src/components/ui/` 本地安装（`packages/ui` 不含）
- 开源项目，代码质量和目录结构需要对外贡献者友好（不依赖任何私有项目）

## Premises

1. Agent-only 主界面（无 Chat 模式）— 后端有 chat 服务但主 UX 是 Agent
2. IPC 封装在 `lib/desktop-api/` 层，组件不直接调 Tauri API
3. Jotai 状态管理，atoms 按功能域分文件
4. 布局：可折叠左侧边栏 + TabBar + 主内容区（多标签页）
5. 清晰模块边界，适合开源贡献

## IPC 桥接方式（已确定）

Tauri `main.rs` 暴露了以下 commands：

```
healthcheck()                          → { ok: true }
sidecar_healthcheck()                  → sidecar healthcheck
sidecar_call(method, params)           → 所有 sidecar RPC 调用的统一入口
open_file_dialog()                     → { files: [...] }
open_folder_dialog()                   → { path: string | null }
open_external(url)                     → void
```

sidecar 通过 **stdio JSON-RPC** 通信。推送事件由 sidecar 发出，Rust 层通过 `app.emit("sidecar:event", { method, params })` 转发到前端。

**前端 IPC 层实现模式：**

```typescript
// lib/desktop-api/agent.ts

import { invoke } from '@tauri-apps/api/core'
import { listen } from '@tauri-apps/api/event'
import type { AgentSendInput, AgentStreamEvent } from '@lume/shared'

// 调用 sidecar 方法（统一入口）
export const agentSend = (input: AgentSendInput) =>
  invoke<{ ok: true }>('sidecar_call', {
    method: 'agent:send-thread-message',
    params: input,
  })

// 监听 sidecar 推送事件（统一事件通道）
// 所有推送事件都走 'sidecar:event'，通过 method 字段路由
export const onSidecarEvent = (
  cb: (method: string, params: unknown) => void
) => listen<{ method: string; params: unknown }>('sidecar:event', (e) =>
  cb(e.payload.method, e.payload.params)
)
```

`useGlobalAgentListeners` hook 在 App.tsx 挂载时注册一个 `sidecar:event` 监听器，根据 `method` 字段分发到对应 Jotai atom 更新。

## Approaches Considered

### Approach A: 直接移植参考项目
把现有参考项目的 renderer 结构搬过来，替换 Electron IPC 为 Tauri invoke/listen，去掉 Chat 模式。
- Effort: M | Risk: Low | Completeness: 6/10
- 问题：IPC 差异大，历史包袱多，对外部贡献者不友好

### Approach B: 从零设计
基于 Lume IPC 类型从零构建，完全匹配后端能力。
- Effort: L | Risk: Med | Completeness: 9/10
- 问题：工作量最大

### Approach C: 混合策略（选定）
架构从零设计（目录结构、IPC 层、atoms 分层），UI 组件和渲染逻辑参考已有实现移植，只取有用的部分。
- Effort: M-L | Risk: Low-Med | Completeness: 8/10

## Recommended Approach

**方案 C — 混合策略**

### 目录结构

```
apps/web/
├── src/
│   ├── main.tsx                    # 入口
│   ├── App.tsx                     # 根组件，初始化 + onboarding 判断
│   │
│   ├── lib/
│   │   ├── desktop-api/            # Tauri IPC 封装层（唯一调用 Tauri 的地方）
│   │   │   ├── agent.ts            # sidecar_call 封装：线程/消息/工作区/文件系统
│   │   │   ├── system.ts           # sidecar_call 封装：settings/channel/system
│   │   │   ├── native.ts           # 原生 Tauri commands：open_file_dialog/open_external
│   │   │   └── index.ts
│   │   └── utils.ts                # cn() 等工具函数
│   │
│   ├── atoms/                      # Jotai 状态（按功能域分文件）
│   │   ├── agent-atoms.ts          # 线程列表、当前线程、streaming 状态、pending 交互
│   │   ├── workspace-atoms.ts      # 工作区列表、当前工作区、capabilities
│   │   ├── tab-atoms.ts            # 标签页状态（见下方 Tab 类型定义）
│   │   ├── sidebar-atoms.ts        # 侧边栏折叠状态
│   │   ├── settings-atoms.ts       # 设置弹窗开关、当前 tab
│   │   └── index.ts
│   │
│   ├── hooks/
│   │   ├── useGlobalAgentListeners.ts   # 注册 sidecar:event 监听，分发到 atoms
│   │   └── useScrollPositionMemory.ts
│   │
│   └── components/
│       ├── app-shell/
│       │   ├── AppShell.tsx        # 主布局：LeftSidebar + MainArea
│       │   ├── LeftSidebar.tsx     # 线程列表 + 工作区切换 + 导航
│       │   └── TitleBar.tsx        # Tauri 自定义标题栏（data-tauri-drag-region）
│       │
│       ├── tabs/
│       │   ├── TabBar.tsx          # 标签页栏
│       │   ├── TabContent.tsx      # 根据 tab.type 渲染对应视图
│       │   └── MainArea.tsx        # TabBar + TabContent 容器
│       │
│       ├── agent/
│       │   ├── AgentView.tsx       # Agent 主视图容器
│       │   ├── AgentHeader.tsx     # 标题 + 工作区选择 + 侧面板切换按钮（文件/Plan）
│       │   ├── AgentMessages.tsx   # 消息列表（普通滚动，不用虚拟滚动，见下方说明）
│       │   ├── SDKContentBlock.tsx # 单条 SDKMessage 渲染（thinking/tool_use/tool_result）
│       │   ├── SubagentCard.tsx    # 子任务内联卡片（嵌入消息流，显示进度+输出）
│       │   ├── AgentInput.tsx      # 输入框（TipTap，支持 @文件 /Skill #MCP mention）+ 附件 + 模型选择
│       │   ├── PermissionBanner.tsx     # 工具权限确认（inline banner）
│       │   ├── AskUserBanner.tsx        # AskUserQuestion（inline banner）
│       │   ├── ExitPlanModeBanner.tsx   # Plan 模式退出确认（见下方说明）
│       │   ├── SidePanel.tsx            # 右侧面板容器（切换逻辑见下）
│       │   ├── PlanPanel.tsx            # Plan 步骤列表
│       │   └── tool-result-renderers/   # 工具结果渲染器
│       │       ├── index.tsx            # dispatcher（根据 toolName 路由）
│       │       ├── bash-result.tsx      # 终端风格深色背景
│       │       ├── read-result.tsx      # 行号代码视图
│       │       ├── edit-result.tsx      # diff 视图（红/绿）
│       │       ├── write-result.tsx     # 成功提示
│       │       ├── grep-result.tsx      # 按文件分组
│       │       ├── glob-result.tsx      # 文件列表
│       │       ├── web-search-result.tsx
│       │       ├── web-fetch-result.tsx
│       │       ├── collapsible-result.tsx  # 超阈值自动折叠包装器
│       │       └── default-result.tsx   # KV 表格或纯文本兜底
│       │
│       ├── file-browser/
│       │   ├── FileBrowser.tsx
│       │   ├── FileTypeIcon.tsx     # 扩展名 → lucide-react 图标映射
│       │   └── FileDropZone.tsx     # 拖拽文件到 thread 工作目录
│       │
│       ├── settings/
│       │   ├── SettingsDialog.tsx
│       │   ├── ChannelSettings.tsx  # API Key / Provider 配置
│       │   ├── AgentSettings.tsx    # 工具策略、代理设置
│       │   ├── McpSettings.tsx      # MCP 服务器管理
│       │   └── AboutSettings.tsx
│       │
│       └── ui/                     # shadcn/ui 组件（button/dialog/tooltip 等）
│
├── index.html
├── vite.config.ts
├── tailwind.config.ts
├── tsconfig.json
└── package.json
```

### Tab 类型定义

```typescript
// atoms/tab-atoms.ts
export type TabType = 'agent' | 'settings'

export interface Tab {
  id: string
  type: TabType
  title: string
  // agent tab 专属
  threadId?: string
  // settings tab 专属
  settingsTab?: 'channel' | 'agent' | 'mcp' | 'about'
}
```

`TabContent.tsx` 根据 `tab.type` 渲染：
- `'agent'` → `<AgentView threadId={tab.threadId} />`
- `'settings'` → `<SettingsDialog defaultTab={tab.settingsTab} />`

### SidePanel 切换逻辑

```typescript
// atoms/agent-atoms.ts
// 每个 threadId 独立维护侧面板状态
export type SidePanelView = 'files' | 'plan' | null

export const agentSidePanelViewAtom = atomWithStorage<Record<string, SidePanelView>>(
  'agent-side-panel-view', {}
)
```

`AgentHeader.tsx` 有两个图标按钮（文件夹/Plan），点击切换对应 `SidePanelView`。`SidePanel.tsx` 根据当前 view 渲染 `FileBrowser` 或 `PlanPanel`，view 为 null 时隐藏。

子任务不在 SidePanel 显示，改为 `SubagentCard` 内联在消息流中（见下方说明）。

### 错误处理策略

| 场景 | 处理方式 |
|------|---------|
| sidecar 启动失败 | App.tsx 初始化时 healthcheck 失败 → 显示错误页面，提示检查环境 |
| sidecar 崩溃（运行中） | `sidecar:event` 停止推送，streaming atom 超时后标记为 errored，AgentView 显示错误 banner |
| IPC 调用超时（45s） | `sidecar_call` 返回 Error，组件 catch 后 toast 提示 |
| IPC 断连（broken pipe） | Rust 层自动重启 sidecar，前端下次调用时透明恢复 |
| Agent 运行时错误 | `STREAM_ERROR` 事件 → `agentStreamingStatesAtom` 标记 errored，AgentView 显示内联错误 |

### 状态管理分层

```
Tauri Event: 'sidecar:event' (单一通道)
    ↓
useGlobalAgentListeners (App.tsx 挂载，根据 method 分发)
    ↓
Jotai atoms 更新
  ├── agent:stream:event      → agentSDKMessagesAtom
  ├── agent:stream:complete   → agentStreamingStatesAtom (idle)
  ├── agent:stream:error      → agentStreamingStatesAtom (errored)
  ├── agent:tool-permission-request → agentPendingPermissionAtom
  ├── agent:ask-user-question → agentPendingAskAtom
  ├── agent:runtime-status-changed → agentRuntimeStatusAtom
  ├── agent:title-updated     → agentThreadsAtom (更新标题)
  ├── agent:plan-state-changed → agentPlanStateAtom
  └── agent:subagent-completed → agentSubagentRunsAtom (更新卡片状态)
    ↓
Components (useAtomValue / useAtom)
```

### 关键 UX 决策

1. **PermissionBanner / AskUserBanner**：inline 放在消息列表和输入框之间，不用全屏遮罩
2. **工具结果折叠**：超过 20 行自动折叠，点击展开
3. **SubagentCard 内联**：子任务直接在消息流中渲染为卡片，不使用右侧 SidePanel。卡片显示任务描述、实时状态（running 蓝色旋转/completed 绿色/errored 红色）、完成后的 `outcome.output` 摘要，可折叠展开。数据来源：`agent:subagent-completed` 事件 → `agentSubagentRunsAtom`
4. **Plan 模式**：独立 PlanPanel，步骤列表 + 当前执行步骤高亮（`PlanStep[]`）
5. **多标签页**：每个 tab 是独立的 Agent 线程，可以同时运行多个任务
6. **FileDropZone**：覆盖整个 AgentView 区域的拖拽目标（`position: absolute, inset: 0`），拖入时显示高亮边框，松手后调用 `sidecar_call('agent:save-files-to-thread', ...)` 上传到 thread 工作目录
7. **AgentMessages 滚动**：不使用虚拟滚动。Agent 消息包含大量富内容（diff、代码高亮），虚拟滚动实现复杂且收益有限（单次会话消息数通常 < 200）。使用普通 `overflow-y: auto` + `useScrollPositionMemory` hook 记忆每个 threadId 的滚动位置（切换 tab 时恢复）
8. **ExitPlanModeBanner**：当 `agentRuntimeStatusAtom.phase === 'review'`（Plan 模式审核阶段）时显示，提供"批准执行"和"取消"两个按钮，批准后调用 `sidecar_call('agent:send-thread-message', { permissionMode: 'acceptEdits' })`
9. **Settings 入口**：设置通过左侧边栏底部的齿轮图标打开，渲染为独立 tab（`type: 'settings'`），不是 Dialog 弹窗。`settings-atoms.ts` 只存储当前激活的 settings tab 名称（`'channel' | 'agent' | 'mcp' | 'about'`），tab 本身的开关状态由 `tab-atoms.ts` 管理

### UI 组件库策略

| 层级 | 库 | 说明 |
|------|-----|------|
| 基础组件 | shadcn/ui | button/dialog/tooltip/input 等，安装在 `apps/web/src/components/ui/` |
| 输入框 | TipTap | `AgentInput`，支持 @文件 /Skill #MCP mention、IME、Enter 提交 |
| 代码高亮 | Shiki（`@lume/ui`） | `CodeBlock` 组件，tool-result-renderers 直接复用 |
| 流式动画 | `useSmoothStream`（`@lume/ui`） | 消息流式渲染平滑效果 |
| Markdown | `@ant-design/x-markdown` | 流式友好，专为 LLM 输出设计；`<code>` 组件替换为 `@lume/ui` CodeBlock |
| 图表 | MermaidBlock（`@lume/ui`） | 按需使用 |
| 图标 | lucide-react | 全局统一 |

不手写任何可以用成熟库替代的交互逻辑。

以下组件需要从零实现，所有逻辑基于 `packages/shared` 的类型定义：

- `lib/desktop-api/` — Tauri invoke/listen 封装
- `App.tsx` — Tauri 初始化流程
- `TitleBar.tsx` — `data-tauri-drag-region` 属性
- `AgentInput.tsx` — 附件上传用 `invoke('open_file_dialog')`
- `SubagentCard.tsx` — 基于 `SubagentRunRecord` 类型，内联在消息流中
- `PlanPanel.tsx` — 基于 `PlanStep[]` 类型
- `useGlobalAgentListeners.ts` — 监听 `sidecar:event`，分发到 atoms

### tool-result-renderers 实现说明

这套渲染器基于 `@lume/agent-sdk` 的 `SDKMessage` 类型，完全独立实现，不依赖任何外部项目：

- `bash-result.tsx`：渲染 `Bash` 工具的 stdout/stderr，终端风格深色背景
- `read-result.tsx`：渲染 `Read` 工具结果，带行号的代码视图
- `edit-result.tsx`：渲染 `Edit` 工具结果，unified diff 格式（红/绿）
- `write-result.tsx`：渲染 `Write` 工具结果，成功提示 + 文件路径
- `grep-result.tsx`：渲染 `Grep` 结果，按文件分组，带 FileTypeIcon
- `glob-result.tsx`：渲染 `Glob` 结果，文件列表，带 FileTypeIcon
- `collapsible-result.tsx`：超过阈值自动折叠的包装器，适用于所有渲染器
- `web-search-result.tsx`：渲染 `WebSearch` 工具结果，卡片列表（title + URL + snippet）
- `web-fetch-result.tsx`：渲染 `WebFetch` 工具结果，Markdown 渲染页面内容

## Open Questions

1. `packages/ui` 里已有哪些组件？如果不包含 shadcn 基础组件，则在 `apps/web/src/components/ui/` 单独安装；如果已包含，则直接从 `@lume/ui` 导入。**临时决策：先在 `apps/web` 本地安装 shadcn，确认 `packages/ui` 范围后再决定是否合并。**
2. 是否需要 onboarding 流程（首次启动引导）？**临时决策：跳过，直接进入主界面，后续迭代再加。**
3. `atomWithStorage` 存储的 `Record<string, SidePanelView>` 会随线程增多无限增长。**临时决策：存储时只保留最近 50 个 threadId，超出时删除最旧的条目。**

## Success Criteria

- [ ] Agent 对话可以正常发送和接收流式响应
- [ ] 工具执行结果有专属渲染器（bash/read/edit/write/grep/glob）
- [ ] 权限确认和 AskUserQuestion 以 inline banner 形式出现
- [ ] 多标签页可以同时运行多个 Agent 线程
- [ ] 文件浏览器可以浏览当前线程工作目录
- [ ] 子任务运行状态以内联卡片形式在消息流中展示
- [ ] Plan 模式步骤列表在 SidePanel 可见，当前执行步骤高亮
- [ ] sidecar 启动失败时显示友好错误页面
- [ ] Agent 运行时错误以内联 banner 形式展示

## Next Steps

1. 确认 `packages/ui` 已有组件范围（避免重复安装 shadcn）
2. 创建 `apps/web` 目录，初始化 Vite + React + TypeScript 项目
3. 配置 Tailwind + shadcn/ui
4. 实现 `lib/desktop-api/` IPC 封装层
5. 搭建 AppShell 布局（LeftSidebar + TabBar + MainArea）
6. 实现 AgentView 核心（消息列表 + 输入框 + 流式渲染）
7. 实现 tool-result-renderers 套件
8. 实现 PermissionBanner + AskUserBanner
9. 实现 SidePanel（FileBrowser + PlanPanel）
10. 实现 SubagentCard 内联卡片
